// Flip to true to bring prices back on storefront display pages (product
// listing, product detail, cross-sell, compare, China landing). Cart,
// checkout, order history, and admin are unaffected — they always show price.
export const SHOW_PRICES = false;
